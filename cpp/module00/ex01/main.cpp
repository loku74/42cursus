#include "PhoneBook.hpp"
#include <iostream>

int main(void)
{
	PhoneBook 	phonebook;
	std::string command;

	while (true)
	{
		std::getline(std::cin, cmd);
		if (std::cin.eof())
			break ;
		if (command == "ADD")

	}
	return (0);
}
